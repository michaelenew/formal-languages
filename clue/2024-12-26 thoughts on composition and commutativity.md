
Was wondering if it's only possible to for some function combo to compose to another function if all the target function's commutativities are satisfied. This is not the case, it's possible for AND and OR to compose to the identity, which commutes with all functions

From above, it seems like it's possible to gain commutativities but not lose them

Open Q: is it possible for a function combo to always gain any commutativities? Basically if the base set has strictly FEWER commutativities (on intersection) than the target, is it always possible for the base set to compose to the target? This would imply every function can compose to the identity (though counterexample exists in the nilary functions...)

Most functions can compose to the identity. Every nondegenerate binary function can. Next step: try to find a function that cannot compose to the identity in isolation
